info:
  name: Upload List
  type: http
  seq: 14

http:
  method: GET
  url: "{{base_url}}/banner/upload"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
