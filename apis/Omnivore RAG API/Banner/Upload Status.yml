info:
  name: Upload Status
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/banner/upload/{{upload_id}}/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
